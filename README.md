# node-fqdn

Get FQDN from host,
Multiplatfotm

## Install
```
npm install node-fqdn --save
```

##Usage

```javascript
var fqdn = require('node-fqdn');

fqdn(function(fqdn_str){
    console.log("FQDN:", fqdn_str);
});
```
